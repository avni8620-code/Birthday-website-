let currentComplimentIndex=0;const questionScreen=document.getElementById('questionScreen');const noScreen=document.getElementById('noScreen');const presentationScreen=document.getElementById('presentationScreen');const yesBtn=document.getElementById('yesBtn');const noBtn=document.getElementById('noBtn');const noToYesBtn=document.getElementById('noToYesBtn');const backBtn=document.getElementById('backBtn');const prevBtn=document.getElementById('prevBtn');const nextBtn=document.getElementById('nextBtn');const pageNumber=document.getElementById('pageNumber');yesBtn.addEventListener('click',goToPresentation);noBtn.addEventListener('click',goToNo);noToYesBtn.addEventListener('click',goToPresentation);backBtn.addEventListener('click',backToQuestion);prevBtn.addEventListener('click',previousCompliment);nextBtn.addEventListener('click',nextCompliment);function showScreen(screen){questionScreen.classList.remove('active');noScreen.classList.remove('active');presentationScreen.classList.remove('active');screen.classList.add('active')}function goToNo(){showScreen(noScreen)}function goToPresentation(){showScreen(presentationScreen);currentComplimentIndex=0;showCompliment(0)}function backToQuestion(){showScreen(questionScreen)}function showCompliment(index){const compliments=document.querySelectorAll('.compliment-slide');compliments.forEach(c=>c.classList.remove('active'));compliments[index].classList.add('active');currentComplimentIndex=index;pageNumber.textContent=index+1}function nextCompliment(){const compliments=document.querySelectorAll('.compliment-slide');let nextIndex=(currentComplimentIndex+1)%compliments.length;showCompliment(nextIndex)}function previousCompliment(){const compliments=document.querySelectorAll('.compliment-slide');let prevIndex=(currentComplimentIndex-1+compliments.length)%compliments.length;showCompliment(prevIndex)}document.addEventListener('keydown',(e)=>{if(presentationScreen.classList.contains('active')){if(e.key==='ArrowRight'||e.key===' ')nextCompliment();else if(e.key==='ArrowLeft')previousCompliment()}})